<context>
Page type: [LANDING PAGE / DASHBOARD / ECOMMERCE / BLOG / APP]
Key content elements: [LIST MAIN COMPONENTS]
Primary user device split: [% MOBILE / % DESKTOP if known]
</context>

<task>
Create a mobile-first responsive design strategy:

1. Mobile layout (320-480px): content priority order, what gets hidden/collapsed, touch target sizes, thumb-zone placement for key actions
2. Tablet layout (768-1024px): what expands, column changes, navigation shifts
3. Desktop layout (1280px+): full layout with sidebar options, hover states, expanded navigation
4. For each breakpoint, specify what content reflows, stacks, or disappears
5. Identify 3 components that need fundamentally different designs per breakpoint (not just resizing)
6. Recommend a navigation pattern that works across all breakpoints

Include specific pixel dimensions and spacing tokens where relevant.
</task>